import NavItems from "./NavItems";

const Nav = () => {
  return (
    <nav className='items-center space-x-8 justify-center hidden md:flex'>
      <NavItems />
    </nav>
  );
};

export default Nav;
