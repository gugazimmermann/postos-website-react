import { Page, Section, SectionTitle } from '../general';
import PrivacityText from './PrivacityText';

const Privacity = () => {
  return (
    <Page>
      <Section>
        <SectionTitle title='Políticas de Privacidade' />
        <PrivacityText />
      </Section>
    </Page>
  );
};

export default Privacity;
