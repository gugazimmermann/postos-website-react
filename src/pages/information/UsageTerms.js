import { Page, Section, SectionTitle } from '../general';
import UsageTermsText from './UsageTermsText';

const UsageTerms = () => {
  return (
    <Page>
      <Section>
        <SectionTitle title='Termos de Uso' />
        <UsageTermsText />
      </Section>
    </Page>
  );
};

export default UsageTerms;
